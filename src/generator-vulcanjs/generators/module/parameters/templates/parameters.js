import { addCallback } from 'meteor/vulcan:core';

// addCallback('<%= parametersName %>', (parameters, terms, apolloClient) => ({
//   selector: parameters.selector,
//   options: parameters.options,
// }));
